{
  "name": "data",
  "version": "1.0.0",
  "description": "database json",
  "scripts": {
    "start": "json-server-auth db.json -r routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0"
  }
}
